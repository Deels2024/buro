# Development/CI only: production downloads the tested web and admin runtimes.
services:
  web:
    build:
      context: ./flutter
      dockerfile: Dockerfile.web.source
  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile.source
