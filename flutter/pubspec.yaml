name: bureau_nakhodok
description: Единая поисковая сеть пропаж и находок России.
publish_to: none
version: 0.2.0+2

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  http: ^1.5.0
  image_picker: ^1.2.3
  qr_flutter: ^4.1.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
